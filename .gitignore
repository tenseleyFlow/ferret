# planning + local guidance (kept local, like aspen)
.docs/
CLAUDE.md

# build artifacts
/ferret
/frt
*.o
*.d
config.mk
config.h

# release tarball (make dist)
/ferret-*.tar.gz

# test/bench scratch
/tests/.work/
/bench/.work/
*.core
